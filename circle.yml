general:
  branches:
    only:
      - master
machine:
  node:
    version: 6.9.1
dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
deployment:
  npm:
    commands:
      - echo "publishing..."
