machine:
  node:
    version: 6.9.1
dependencies:
  pre:
    - echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
deployment:
  npm:
    branch: master
    commands:
      - echo "publishing..."
